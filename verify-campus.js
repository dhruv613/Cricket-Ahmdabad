(async()=>{window.scrollTo({top:0,behavior:'instant'});document.getElementById('explore-campus').click();const host=document.getElementById('campus');const api=host._api;const results=[];const settle=async()=>{const end=performance.now()+20000;do{await new Promise(r=>setTimeout(r,150));if(!api.getDiagnostics().moving)return;}while(performance.now()<end);throw Error('Camera did not settle');};const check=(name,value)=>{if(!value)throw Error(name);results.push({name,passed:true})};check('Pinned local Three.js renderer',api.getDiagnostics().three==='184');check('Detailed geometry is rendered',api.getDiagnostics().triangles>100000);check('Five accessible hotspots',host.querySelectorAll('.campus-hotspot[aria-label]').length===5);host.querySelector('[data-action="night"]').click();check('Floodlights mode activates',api.getDiagnostics().timeOfDay==='night'&&host.querySelector('[data-action="night"]').getAttribute('aria-pressed')==='true');host.querySelector('[data-action="day"]').click();check('Daylight mode restores',api.getDiagnostics().timeOfDay==='day');api.setSport('ground');check('Ground camera selection',api.getDiagnostics().selected==='ground');await settle();const before=api.getDiagnostics();host.querySelector('[data-action="in"]').click();await settle();const after=api.getDiagnostics();const distance=d=>Math.hypot(...d.camera.map((v,i)=>v-d.target[i]));check('Zoom moves the perspective camera closer',distance(after)<distance(before));host.querySelector('[data-action="aerial"]').click();check('Aerial view clears facility focus',api.getDiagnostics().selected===null);return results})()
